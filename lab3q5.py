'''
Do not remove any text from these comments
5.	Create a variable called secret_num and set the value to 50. 
Ask the user to enter a number. While their guess is not the
same as the secret_num, tell them if their guess is too low or 
too high, and ask them to have another guess. If they enter the same 
value as secret_num, display the message 'Well done, you took [count] attempts'.

Function to uses: str(), input(), int(), print()
Operator: !=, <, +
Structure: while, if - else
'''
def main():


    return 0

